<h1 class="new-comment-header">Add Comment</h1>
<div class="new-comment-form">
	<p>Add Comment </p>
	<%= simple_form_for @comment do |c| %>
		<%= c.text_area :body, input_html: { maxlength: 500 } %><br>
		<%= c.submit :class => "btn btn-success" %>
	<% end %>
</div>